function RestartGame({restart}) {
    return (
        <div className="container container-restart">
            <button onClick={restart}>Restart Game</button>
        </div>
    );
}

export default RestartGame;
